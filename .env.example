# Connection string Postgres (Render fornece essa variável automaticamente
# quando o serviço web é ligado à database via render.yaml).
# Local: aponte para um Postgres local ou use um banco gratuito (Neon, Supabase).
DATABASE_URL="postgresql://user:password@localhost:5432/sisrenda?schema=public"
